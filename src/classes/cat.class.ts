export class Cat {
    constructor (
        name: string, 
        type: string
    ) {
        this.name = name;
        this.type = type;
    }

    public id: number;
    public name: string;
    public type: string;
}